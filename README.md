# Kursova_NVBD_Variant_02
Комплексна BI-система для аналізу комунальних платежів та управління нерухомістю. Проєкт розроблено в рамках курсової роботи з дисципліни "Надвеликі бази даних" (НВБД).

📋 Про проєкт
Система призначена для автоматизації збору, обробки та візуалізації фінансових даних (інвойсів) для компанії, що керує нерухомістю. Вона замінює ручну обробку Excel-файлів на автоматизований DWH-конвеєр, що дозволяє аналізувати дані в розрізі років, регіонів та типів послуг за лічені секунди.

Основні можливості:

Автоматичний імпорт даних з CSV-файлів.

Історичний аналіз доходів та заборгованостей.

Інтерактивні дашборди для керівництва.

Прогнозування та KPI (виконання плану).

🏗 Архітектура
Проєкт реалізовано на стеку технологій Microsoft SQL Server BI Stack:

Database Engine: Зберігання "сирих" даних та Data Warehouse (Схема "Зірка").

SSIS (Integration Services): ETL-процеси. Очищення даних, інкрементальне завантаження, обробка помилок.

SSAS (Analysis Services): Multidimensional OLAP-куб. Розрахунок метрик (YTD, YoY), ієрархії.

SSRS (Reporting Services): Візуалізація. Матричні звіти, графіки, Drill-down, експорт у PDF/Excel.
Інструкція з розгортання
Для запуску проєкту необхідні: MS SQL Server 2019+, Visual Studio 2019+ (з SSDT), Excel (опціонально).

Крок 1. База даних
Відкрийте SSMS.

Запустіть скрипт Database/01_Create_DWH.sql для створення структури сховища.

Запустіть Database/02_Populate_DimDate.sql для наповнення календаря.

Крок 2. ETL (Наповнення даними)
Відкрийте проєкт з папки SSIS у Visual Studio.

Налаштуйте Connection Managers (вкажіть ваш локальний сервер SQL).

Запустіть пакет Main.dtsx.

Примітка: Пакет згенерує або завантажить тестові дані (500k+ записів).

Крок 3. OLAP-куб
Відкрийте проєкт SSAS.

Змініть Data Source на ваш сервер.

Виконайте Deploy проєкту.

Натисніть Process Full, щоб розрахувати агрегації.

Крок 4. Звітність
Відкрийте проєкт SSRS.

Запустіть звіти локально (Preview) або задеплойте їх на Report Server.

Перейдіть на http://localhost/reports для перегляду.
